{
  "name": "neon-dash",
  "version": "1.0.0",
  "description": "Neon Dash — a one-thumb gravity-flip endless arcade game (mobile PWA).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
